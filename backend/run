echo "Running migrations..."
alembic upgrade head
echo "Running server..."

if [ $STAGE == "local" ]
then
    echo "Run with --reload"
    uvicorn main:app --reload
else
    uvicorn --host 0.0.0.0 main:app
fi